{
  "name": "lottery-prediction",
  "version": "1.0.0",
  "scripts": {
    "fetch-data": "node src/fetch-lottery-data.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp src/lottery-data.json dist/",
    "deploy": "deploy.bat"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0",
    "http-server": "^14.1.1"
  }
}
